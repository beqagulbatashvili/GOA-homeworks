const names = ["Alex", "Sophie", "Daniel", "Chris", "Emma"];
const result = names.join(" | ");
console.log(result);



function myAbs(number) {
    if (number < 0) {
        return -number;
    } else {
        return number; 
    }
}


const largest = Math.max(-5, -12, -3, -20);

console.log(largest);
